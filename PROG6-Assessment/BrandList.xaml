<Window x:Class="PROG6_Assessment.BrandList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BrandList" Height="300" Width="300">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Appie Merken" FontSize="28" TextAlignment="Center"/>
        <DataGrid DockPanel.Dock="Top" AutoGenerateColumns="False" IsReadOnly="True" Margin="5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Merk" Width="300" />
                <DataGridTextColumn Header="Aantal" Width="50" />
                <DataGridTextColumn Header="Prijs" Width="*" />
                <DataGridTextColumn Header="Korting" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel DockPanel.Dock="Bottom">
            <Button>Merk Bewerken</Button>
            <Button>Merk Verwijderen</Button>
            <Button Margin="0,20,0,0" >Merk toevoegen</Button>
        </StackPanel>
        <TextBlock DockPanel.Dock="Bottom" Text="Totaalprijs: " FontSize="14" Padding="5,0,0,0"/>
    </DockPanel>
</Window>
