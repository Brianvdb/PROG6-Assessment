<Window x:Class="PROG6_Assessment.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525"
        DataContext="{Binding Source={StaticResource Locator}, Path=Appie}">
    <StackPanel>
        <Button Command="{Binding OpenShoppingList}" Width="150" HorizontalAlignment="Right" Margin="0,5,5,0">Open Boodschappenlijstje</Button>
    </StackPanel>
</Window>
